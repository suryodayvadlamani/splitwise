{
  "name": "splitwise-like",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.1.26",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"api,web\" -c \"cyan,magenta\" \"cd apps/api && bun run dev\" \"cd apps/web && bun run dev\"",
    "dev:api": "cd apps/api && bun run dev",
    "dev:web": "cd apps/web && bun run dev",
    "dev:mobile": "cd apps/mobile && bun run dev",
    "dev:all": "concurrently -n \"api,web,mobile\" -c \"cyan,magenta,green\" \"cd apps/api && bun run dev\" \"cd apps/web && bun run dev\" \"cd apps/mobile && bun run dev\"",
    "build": "bun --cwd packages/types run build && bun --cwd packages/db run build && bun --cwd apps/api run build && bun --cwd apps/web run build",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules **/dist **/.vite",
    "install:all": "bun install --frozen-lockfile",
    "typecheck": "echo 'Run type checks within each workspace'"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.6.3"
  }
}
